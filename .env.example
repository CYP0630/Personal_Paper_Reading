# Copy to a secret environment file on the runtime machine. Never commit values.
HF_TOKEN=

# NCBI asks API clients to identify a valid contact email.
PAPER_RADAR_EMAIL=

# Optional; useful only when PubMed request volume grows.
NCBI_API_KEY=

